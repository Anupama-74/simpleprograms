#include <stdio.h>
int main()
{
    char s[1000];
    int i;
    print("Enter a string for which you need to find the length: ")
    scan("%s", s)
    for(i = 0; s[i] != '\0'; ++i)
    printf("Length of the provided string: %d", i);
    
}
